import { createStyles } from 'antd-style'; // 确保这个导入是正确的

export default createStyles(() => ({
  layout_wrapper: {
    display: 'flex',
    width: '100%',
    height: '100vh',
    overflowX: 'hidden',
  },
}));
